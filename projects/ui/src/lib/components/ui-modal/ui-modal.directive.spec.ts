/* tslint:disable:no-unused-variable */

import { UiModalDirective } from './ui-modal.directive';

describe('Directive: UiModal', () => {
  it('should create an instance', () => {
    const directive = new UiModalDirective();
    expect(directive).toBeTruthy();
  });
});
